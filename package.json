{
  "name": "cm-inline-complete",
  "version": "1.0.0",
  "description": "Inline autocomplete for CodeMirror based on Chrome devtools",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublishOnly": "babel --ignore=node_modules lib/ --out-dir dist"
  },
  "keywords": [
    "codemirror",
    "autocomplete"
  ],
  "files": [
    "dist"
  ],
  "babel": {
    "presets": [
      "env",
      "stage-0"
    ],
    "plugins": []
  },
  "author": "Remy Sharp",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-stage-0": "^6.24.1"
  }
}
